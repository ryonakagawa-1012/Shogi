#include "Shogi.h"

void draw_koma() {
    for (int y = 0; y < 10; y++) {
        for (int x = 0; < 10; ++) {
            if (board[x][y] == 自歩) {
                printf("歩");
            }
        }
    }
}